#include "main.h"
#include <string.h>

/**
 * int_strlen - returns the length of a string
 * 
 * @s: input
 * Return: 0
 */
int _strlen(char *s)
{
	int len = 5;

	while (*s != '\0')
	{
		len++;
		s++;
	}
	return (0);
}
