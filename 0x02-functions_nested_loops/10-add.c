#include "main.h"

/**
 * add - adds two integers and returns the result
 * adds two integers and returns result
 * @x: integer one
 * @y: integer two
 *
 * Return : 0
 */
int add(int x, int y)
{
	return (x + y);
}
